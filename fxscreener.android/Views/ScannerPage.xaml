<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:converters="clr-namespace:fxscreener.android.Converters"
             x:Class="fxscreener.android.Views.ScannerPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <ContentPage.Resources>
        <converters:IntToStringConverter x:Key="IntToStringConverter" />
        <converters:InverseBoolConverter x:Key="InverseBoolConverter" />

        <!-- Стиль для всех ячеек грида -->
        <Style x:Key="GridCellLabel" TargetType="Label">
            <Setter Property="VerticalOptions" Value="Center"/>
            <Setter Property="HorizontalOptions" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="LineBreakMode" Value="NoWrap"/>
            <Setter Property="TextColor" Value="{DynamicResource TextColor}"/>
        </Style>

        <!-- Стиль для заголовков -->
        <Style x:Key="GridHeaderLabel" TargetType="Label" BasedOn="{StaticResource GridCellLabel}">
            <Setter Property="FontAttributes" Value="Bold"/>
            <Setter Property="BackgroundColor" Value="{DynamicResource Primary}"/>
            <Setter Property="TextColor" Value="{DynamicResource HeaderTextColor}"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>

        <!-- Стиль для фреймов UD и Ws ячеек -->
        <Style x:Key="SignalFrame" TargetType="Frame">
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="HasShadow" Value="False"/>
            <Setter Property="HorizontalOptions" Value="Fill"/>
            <Setter Property="VerticalOptions" Value="Fill"/>
        </Style>
    </ContentPage.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с меню -->
        <Grid Grid.Row="0" 
              BackgroundColor="{DynamicResource Primary}"
              HeightRequest="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ImageButton Grid.Column="0"
                         BackgroundColor="Transparent"
                         WidthRequest="40"
                         HeightRequest="40"
                         HorizontalOptions="Start"
                         Clicked="OnMenuButtonClicked">
                <ImageButton.Source>
                    <FontImageSource FontFamily="MaterialIcons"
                                     Glyph=""
                                     Color="White"
                                     Size="24"/>
                </ImageButton.Source>
            </ImageButton>

            <Label Grid.Column="1"
                   Text="FX Screener"
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"/>
        </Grid>

        <!-- Панель обновления -->
        <HorizontalStackLayout Grid.Row="1" 
                               Padding="10,5"
                               Spacing="10"
                               HorizontalOptions="Center">
            <Button Text="Обновить" 
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                    BackgroundColor="{DynamicResource Primary}"
                    TextColor="White"
                    FontSize="12"
                    HeightRequest="35"/>
            <Label Text="{Binding LastUpdateTimeFormatted, StringFormat='Обн: {0}'}"
                   VerticalOptions="Center"
                   TextColor="Gray"
                   FontSize="11"/>
        </HorizontalStackLayout>

        <!-- Индикатор загрузки -->
        <AbsoluteLayout Grid.Row="2" 
                        IsVisible="{Binding IsLoading}"
                        BackgroundColor="#80000000"
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill">
            <ActivityIndicator IsRunning="True"
                               AbsoluteLayout.LayoutBounds="0.5, 0.5, 50, 50"
                               AbsoluteLayout.LayoutFlags="PositionProportional"
                               Color="{DynamicResource Primary}"/>
        </AbsoluteLayout>

        <!-- Грид с результатами -->
        <ScrollView Grid.Row="2" 
                    Orientation="Horizontal"
                    HorizontalScrollBarVisibility="Always">

            <CollectionView ItemsSource="{Binding DisplayRows}">

                <!-- Шапка таблицы -->
                <CollectionView.Header>
                    <Grid Padding="0,1" 
                          ColumnSpacing="1" 
                          RowSpacing="1"
                          BackgroundColor="{DynamicResource PageBackgroundColor}">

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="45"/>
                            <ColumnDefinition Width="45"/>
                            <ColumnDefinition Width="55"/>
                            <ColumnDefinition Width="55"/>
                            <ColumnDefinition Width="55"/>
                        </Grid.ColumnDefinitions>

                        <!-- Name -->
                        <Label Grid.Column="0" 
                               Text="Имя"
                               Style="{StaticResource GridHeaderLabel}"/>

                        <!-- Period -->
                        <Label Grid.Column="1" 
                               Text="Пер."
                               Style="{StaticResource GridHeaderLabel}"/>

                        <!-- C5 -->
                        <Label Grid.Column="2" 
                               Text="C5"
                               Style="{StaticResource GridHeaderLabel}"/>

                        <!-- F2 -->
                        <Label Grid.Column="3" 
                               Text="F2"
                               Style="{StaticResource GridHeaderLabel}"/>

                        <!-- W5e/W21e -->
                        <Label Grid.Column="4"
                               Text="WPR"
                               Style="{StaticResource GridHeaderLabel}"/>

                        <!-- UD5/UD21 -->
                        <Label Grid.Column="5"
                               Text="UD"
                               Style="{StaticResource GridHeaderLabel}"/>

                        <!-- Ws5/Ws21 -->
                        <Label Grid.Column="6"
                               Text="Ws"
                               Style="{StaticResource GridHeaderLabel}"/>
                    </Grid>
                </CollectionView.Header>

                <!-- Шаблон строки данных -->
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,1" 
                              ColumnSpacing="1" 
                              RowSpacing="1"
                              BackgroundColor="{Binding PairColor}">

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="70"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="45"/>
                                <ColumnDefinition Width="45"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                                <ColumnDefinition Width="55"/>
                            </Grid.ColumnDefinitions>

                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnInstrumentTapped" NumberOfTapsRequired="2"/>
                            </Grid.GestureRecognizers>

                            <!-- Name (только первая строка) -->
                            <Label Grid.Column="0" 
                                   Text="{Binding Name}"
                                   Style="{StaticResource GridCellLabel}"
                                   FontAttributes="Bold"/>

                            <!-- Period (только первая строка) -->
                            <Label Grid.Column="1" 
                                   Text="{Binding Period}"
                                   Style="{StaticResource GridCellLabel}"/>

                            <!-- C5 (только первая строка) -->
                            <Label Grid.Column="2" 
                                   Text="{Binding C5}"
                                   Style="{StaticResource GridCellLabel}"/>

                            <!-- F2 (только первая строка) -->
                            <Label Grid.Column="3" 
                                   Text="{Binding F2}"
                                   Style="{StaticResource GridCellLabel}"/>

                            <!-- W5e/W21e -->
                            <Label Grid.Column="4"
                                   Text="{Binding WprDisplay}"
                                   Style="{StaticResource GridCellLabel}"
                                   TextColor="{Binding WprTextColor}"
                                   FontAttributes="Bold"/>

                            <!-- UD5/UD21 -->
                            <Frame Grid.Column="5"
                                   Style="{StaticResource SignalFrame}"
                                   BackgroundColor="{Binding UdBackgroundColor}">
                                <Label Text="{Binding UdDisplay}"
                                       Style="{StaticResource GridCellLabel}"
                                       FontAttributes="Bold"/>
                            </Frame>

                            <!-- Ws5/Ws21 -->
                            <Frame Grid.Column="6"
                                   Style="{StaticResource SignalFrame}"
                                   BackgroundColor="{Binding WsBackgroundColor}">
                                <Label Text="{Binding WsDisplay}"
                                       Style="{StaticResource GridCellLabel}"
                                       FontAttributes="Bold"/>
                            </Frame>

                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </ScrollView>

        <!-- Нижняя панель статуса -->
        <Border Grid.Row="3"
                StrokeThickness="1"
                Stroke="{DynamicResource Primary}"
                Padding="8"
                Margin="5">
            <Label Text="{Binding StatusMessage}"
                   FontSize="10"
                   HorizontalOptions="Center"/>
        </Border>
    </Grid>
</ContentPage>