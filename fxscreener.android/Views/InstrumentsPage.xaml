<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:fxscreener.android.ViewModels"
             x:Class="fxscreener.android.Views.InstrumentsPage"
             BackgroundColor="{DynamicResource PageBackgroundColor}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Grid Grid.Row="0" 
              BackgroundColor="{DynamicResource Primary}"
              HeightRequest="50">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ImageButton Grid.Column="0"
                         BackgroundColor="Transparent"
                         WidthRequest="40"
                         HeightRequest="40"
                         HorizontalOptions="Start"
                         Clicked="OnBackButtonClicked">
                <ImageButton.Source>
                    <FontImageSource FontFamily="MaterialIcons"
                                     Glyph="&#xe5c4;"
                                     Color="White"
                                     Size="24"/>
                </ImageButton.Source>
            </ImageButton>

            <Label Grid.Column="1"
                   Text="Управление инструментами"
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"/>
        </Grid>

        <!-- Список инструментов -->
        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Instruments}"
                        SelectionMode="None"
                        Margin="10">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItem Text="Удалить" 
                                       BackgroundColor="Red"
                                       Command="{Binding DeleteInstrumentCommand, Source={RelativeSource AncestorType={x:Type viewmodels:InstrumentsViewModel}}}"
                                       CommandParameter="{Binding .}"/>
                        </SwipeView.RightItems>

                        <!-- Простая горизонтальная верстка для отображения -->
                        <HorizontalStackLayout Spacing="15"
                                               Padding="10,5"
                                               BackgroundColor="White"
                                               Margin="0,2">

                            <!-- Имя инструмента (жирным) -->
                            <Label Text="{Binding Symbol}" 
                                   FontSize="16" 
                                   FontAttributes="Bold"
                                   VerticalOptions="Center"
                                   WidthRequest="80"/>

                            <!-- Период -->
                            <Label Text="{Binding Period}" 
                                   FontSize="14"
                                   VerticalOptions="Center"
                                   WidthRequest="50"/>

                            <!-- Дата последнего обновления -->
                            <Label Text="{Binding LastUpdated, StringFormat='{0:dd.MM.yyyy HH:mm}'}"
                                   FontSize="12"
                                   TextColor="Gray"
                                   VerticalOptions="Center"
                                   HorizontalOptions="EndAndExpand"/>
                        </HorizontalStackLayout>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Кнопка добавления -->
        <Button Grid.Row="2"
                Text="➕ Добавить инструмент"
                Command="{Binding AddInstrumentCommand}"
                BackgroundColor="{DynamicResource Primary}"
                TextColor="White"
                Margin="10"
                IsEnabled="{Binding IsNotLoading}"/>

        <!-- Индикатор загрузки -->
        <AbsoluteLayout Grid.Row="1"
                        Grid.RowSpan="2"
                        IsVisible="{Binding IsLoading}"
                        BackgroundColor="#80000000"
                        HorizontalOptions="Fill"
                        VerticalOptions="Fill">
            <ActivityIndicator IsRunning="True"
                               AbsoluteLayout.LayoutBounds="0.5, 0.5, 50, 50"
                               AbsoluteLayout.LayoutFlags="PositionProportional"
                               Color="{DynamicResource Primary}"/>
        </AbsoluteLayout>

        <!-- Статус -->
        <Border Grid.Row="3"
                StrokeThickness="1"
                Stroke="{Binding StatusColor}"
                Padding="8"
                Margin="5">
            <Label Text="{Binding StatusMessage}"
                   TextColor="{Binding StatusColor}"
                   FontSize="11"
                   HorizontalOptions="Center"/>
        </Border>
    </Grid>
</ContentPage>