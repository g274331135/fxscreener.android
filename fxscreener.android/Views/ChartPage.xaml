<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="fxscreener.android.Views.ChartPage"
             Title="{Binding Title}"
             BackgroundColor="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопкой назад (опционально, можно использовать стандартную навигацию) -->
        <Grid Grid.Row="0" BackgroundColor="{DynamicResource Primary}" HeightRequest="50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ImageButton Grid.Column="0"
                         BackgroundColor="Transparent"
                         WidthRequest="40"
                         HeightRequest="40"
                         HorizontalOptions="Start"
                         Command="{Binding CloseCommand}">
                <ImageButton.Source>
                    <FontImageSource FontFamily="MaterialIcons"
                                     Glyph="&#xe5c4;"
                                     Color="White"
                                     Size="24"/>
                </ImageButton.Source>
            </ImageButton>

            <Label Grid.Column="1"
                   Text="{Binding Title}"
                   TextColor="White"
                   FontSize="18"
                   FontAttributes="Bold"
                   VerticalOptions="Center"
                   HorizontalOptions="Start"/>
        </Grid>

        <!-- Контейнер для графика (SKCanvasView будет добавлен программно) -->
        <ContentView x:Name="ChartContainer" Grid.Row="1" />

        <!-- Индикатор загрузки -->
        <ActivityIndicator Grid.Row="1"
                           IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           HorizontalOptions="Center"
                           VerticalOptions="Center"/>
    </Grid>
</ContentPage>