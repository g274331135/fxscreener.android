<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:fxscreener.android.Views"
       x:Class="fxscreener.android.AppShell">

    <!-- Регистрируем маршруты для страниц -->
    <ShellContent Title="Scanner" 
                  Route="scanner" 
                  ContentTemplate="{DataTemplate views:ScannerPage}" />

    <ShellContent Title="Settings" 
                  Route="settings" 
                  ContentTemplate="{DataTemplate views:SettingsPage}" />

    <ShellContent Title="Instruments" 
                  Route="instruments" 
                  ContentTemplate="{DataTemplate views:InstrumentsPage}" />

</Shell>